[package]
name = "cycle-helper"
version = "0.1.0"
authors = ["a <a@be.com"]
edition = "2018"
license = "MIT"
description = "fixture helper with a dev-dependency back edge"
repository = "github.com/paritytech/cargo-dragons"

[dev-dependencies]
cycle-consumer = { version = "0.1.0", path = "../cycle-consumer" }
