[package]
name = "app"
version = "0.1.0"
authors = ["a <a@be.com"]
edition = "2018"
license = "MIT"
description = "fixture app with a dev-only path dependency"
repository = "github.com/paritytech/cargo-dragons"

[dev-dependencies]
dev-helper = { version = "0.1.0", path = "../dev-helper" }
